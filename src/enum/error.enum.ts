export enum HashupError {
	BALANCE = 'execution reverted: ERC20: transfer amount exceeds balance',
	DISMISSAL = 'execution cancelled: user dismissed transaction',
	NETWORK = 'execution aborted: network is invalid',
}
